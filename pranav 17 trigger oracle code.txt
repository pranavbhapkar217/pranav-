BEGIN
   EXECUTE IMMEDIATE 'DROP TABLE Employee';
EXCEPTION
   WHEN OTHERS THEN NULL;
END;
/

CREATE TABLE Employee (
    emp_id NUMBER PRIMARY KEY,
    emp_name VARCHAR2(50),
    salary NUMBER
);

CREATE OR REPLACE TRIGGER trg_default_salary
BEFORE INSERT ON Employee
FOR EACH ROW
BEGIN
    IF :NEW.salary IS NULL OR :NEW.salary = 0 THEN
        :NEW.salary := 10000;
    END IF;
END;
/

INSERT INTO Employee (emp_id, emp_name) VALUES (1, 'Rahul');
INSERT INTO Employee (emp_id, emp_name, salary) VALUES (2, 'Sneha', 0);
INSERT INTO Employee (emp_id, emp_name, salary) VALUES (3, 'Amit', 25000);

COMMIT;

SELECT * FROM Employee;